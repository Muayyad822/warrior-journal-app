{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/((?!api|_next|favicon.ico|manifest.json|sw.js|robots.txt|sitemap.xml).*)",
      "destination": "/index.html"
    }
  ]
}
